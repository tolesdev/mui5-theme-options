{
  "root": "apps/mui-system",
  "sourceRoot": "apps/mui-system",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/mui-system",
        "outputPath": "dist/apps/mui-system"
      },
      "configurations": {
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "options": {
        "buildTarget": "mui-system:build",
        "dev": true
      },
      "configurations": {
        "production": {
          "buildTarget": "mui-system:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "mui-system:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/mui-system"],
      "options": {
        "jestConfig": "apps/mui-system/jest.config.js",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/mui-system/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
