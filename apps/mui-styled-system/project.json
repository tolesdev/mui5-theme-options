{
  "root": "apps/mui-styled-system",
  "sourceRoot": "apps/mui-styled-system",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/mui-styled-system",
        "outputPath": "dist/apps/mui-styled-system"
      },
      "configurations": {
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "options": {
        "buildTarget": "mui-styled-system:build",
        "dev": true
      },
      "configurations": {
        "production": {
          "buildTarget": "mui-styled-system:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "mui-styled-system:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/mui-styled-system"],
      "options": {
        "jestConfig": "apps/mui-styled-system/jest.config.js",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/mui-styled-system/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
